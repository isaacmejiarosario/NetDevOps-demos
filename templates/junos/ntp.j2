<configuration>
    <system>
        <ntp operation = "replace">
        {% for ntp in host.facts.ntp_servers %}
            <server>
                <name>{{ ntp }}</name>
            </server>
        {% endfor %}
        </ntp>
    </system>
</configuration>

